users:
  joe: "bcrypt_password"

groups:
  admins:
    - joe

rules:
  - permit: ALLOW
    principal: "user:joe"
    pattern: ".+"

  - permit: ALLOW
    principal: "group:everyone"
    pattern: "public\\/[a-zA-Z0-9]+\\.[a-z]"